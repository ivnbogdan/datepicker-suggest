/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: "/datepicker-suggest",
    output: "export",  // <=== enables static exports
    reactStrictMode: true,
};

export default nextConfig;
